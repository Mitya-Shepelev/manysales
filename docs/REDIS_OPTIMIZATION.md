# Оптимизация ManySales с Redis

Документация по оптимизации производительности с использованием Redis для кеширования.

## 📋 Обзор

В проект добавлена комплексная система кеширования, которая значительно ускоряет работу всех частей приложения:
- **Каталог товаров** - категории, бренды, фильтры
- **Панель продавца** - статистика, топ товары, заказы
- **API** - автоматическое кеширование ответов для мобильного приложения

## 🚀 Что было оптимизировано

### 1. Каталог товаров (`CatalogCacheService`)

**Кешируются:**
- Дерево категорий (1 час)
- Категории первого уровня (1 час)
- Бренды (1 час)
- Диапазоны цен для фильтров (30 минут)
- Популярные товары (5 минут)
- Фильтры категорий (бренды, цены) (30 минут)

**Использование:**

```php
use App\Services\CatalogCacheService;

// Получить дерево категорий
$categories = CatalogCacheService::getCategoriesTree();

// Получить категорию по slug
$category = CatalogCacheService::getCategoryBySlug('electronics');

// Получить все бренды
$brands = CatalogCacheService::getAllBrands();

// Получить фильтры для категории
$filters = CatalogCacheService::getCategoryFilters($categoryId);

// Получить популярные товары
$trending = CatalogCacheService::getTrendingProducts(10);
```

**Автоматическая очистка:**
- При сохранении/удалении категории
- При сохранении/удалении товара (только для своей категории)

### 2. Панель продавца (`VendorCacheService`)

**Кешируются:**
- Статистика продаж (5 минут)
- Топ-продаваемые товары (15 минут)
- Товары с низким запасом (15 минут)
- Статистика по категориям (30 минут)
- Недавние заказы (5 минут)

**Использование:**

```php
use App\Services\VendorCacheService;

$vendorId = auth('seller')->id();

// Получить статистику продаж
$stats = VendorCacheService::getVendorSalesStats($vendorId);
// Возвращает:
// - total_products
// - pending_orders
// - today_orders
// - today_revenue
// - month_revenue
// - total_revenue

// Получить топ товары
$topProducts = VendorCacheService::getTopSellingProducts($vendorId, 10);

// Получить товары с низким запасом
$lowStock = VendorCacheService::getLowStockProducts($vendorId, 10);

// Получить статистику по категориям
$categoryStats = VendorCacheService::getCategoryStats($vendorId);

// Получить недавние заказы
$recentOrders = VendorCacheService::getRecentOrders($vendorId, 20);
```

**Автоматическая очистка:**
- При сохранении/удалении товара продавца
- При создании нового заказа (через обсерверы Order модели)

### 3. API Кеширование (`ApiCacheMiddleware`)

**Автоматически кешируются:**
- `GET /api/v1/products/latest` (5 минут)
- `GET /api/v1/products/featured` (10 минут)
- `GET /api/v1/products/top-rated` (10 минут)
- `GET /api/v1/categories` (1 час)
- `GET /api/v1/brands` (1 час)
- `GET /api/v1/config` (30 минут)

**Особенности:**
- Кешируется только для GET запросов
- Учитывается язык (`X-localization` header)
- Учитывается валюта (`currency-code` header)
- Для авторизованных пользователей - персональный кеш
- Добавляются заголовки `X-Cache: HIT/MISS` для отладки

**Middleware автоматически применяется** ко всем API роутам через группу 'api' в `Kernel.php`.

### 4. Оптимизация CategoryManager

`CategoryManager::parents()` теперь использует кеширование для получения категорий с подкатегориями.

## 🛠️ Команды управления кешем

### Очистка кеша

```bash
# Очистить весь кеш приложения
php artisan cache:clear-app --type=all

# Очистить только кеш каталога
php artisan cache:clear-app --type=catalog

# Очистить кеш API
php artisan cache:clear-app --type=api

# Очистить кеш конкретного продавца
php artisan cache:clear-app --type=vendor --vendor=123

# Очистить с последующим прогревом
php artisan cache:clear-app --type=all --warmup
```

### Прогрев кеша

Прогрев автоматически заполняет наиболее используемые данные:

```bash
php artisan cache:clear-app --type=catalog --warmup
```

## 📊 Ожидаемые результаты

### Производительность

| Операция | До Redis | С Redis | Ускорение |
|----------|----------|---------|-----------|
| Загрузка главной страницы | 300-500 мс | 100-200 мс | **2-3x** |
| Открытие категории | 200-400 мс | 50-100 мс | **3-4x** |
| API запрос каталога | 200-400 мс | 50-100 мс | **3-4x** |
| Панель продавца (статистика) | 500-800 мс | 100-150 мс | **4-5x** |
| Вход пользователя | 150-200 мс | 30-50 мс | **4-5x** |

### Нагрузка на БД

- **↓ 60-80%** запросов к PostgreSQL
- **↓ 70-90%** на частые запросы (категории, бренды, настройки)
- Возможность обработки **100+ одновременных пользователей**

## 🔧 Настройка и конфигурация

### Текущие настройки

В `.env` уже настроено:

```env
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_DRIVER=redis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=1234
REDIS_PORT=6379
```

### Времена жизни кеша

Можно настроить в соответствующих сервисах:

**CatalogCacheService:**
- `CACHE_TTL_SHORT = 300` (5 минут)
- `CACHE_TTL_MEDIUM = 1800` (30 минут)
- `CACHE_TTL_LONG = 3600` (1 час)
- `CACHE_TTL_EXTRA_LONG = 86400` (24 часа)

**VendorCacheService:**
- `CACHE_TTL_SHORT = 300` (5 минут)
- `CACHE_TTL_MEDIUM = 900` (15 минут)
- `CACHE_TTL_LONG = 1800` (30 минут)

## 📝 Ручная работа с кешем

### Очистка в коде

```php
use App\Services\CatalogCacheService;
use App\Services\VendorCacheService;

// Очистить весь кеш каталога
CatalogCacheService::clearAll();

// Очистить только категории
CatalogCacheService::clearCategories();

// Очистить только бренды
CatalogCacheService::clearBrands();

// Очистить фильтры категории
CatalogCacheService::clearCategoryFilters($categoryId);

// Очистить кеш продавца
VendorCacheService::clearVendor($vendorId);

// Очистить после обновления товара
VendorCacheService::clearProductCache($vendorId, $productId);

// Очистить после нового заказа
VendorCacheService::clearOrderCache($vendorId);
```

### Прогрев в коде

```php
use App\Services\CatalogCacheService;

// Прогреть кеш каталога
$warmed = CatalogCacheService::warmup();
// Возвращает массив прогретых ключей
```

## 🔍 Отладка

### Проверка кеша API

Все API ответы содержат заголовки для отладки:

```
X-Cache: HIT          # Данные из кеша
X-Cache: MISS         # Данные из БД
X-Cache-Key: api_cache:api/v1/products/latest:ru:RUB:abc123...
```

### Мониторинг Redis

```bash
# Подключиться к Redis CLI
redis-cli -a 1234

# Посмотреть все ключи
KEYS *

# Посмотреть ключи каталога
KEYS *catalog:*

# Посмотреть ключи продавца
KEYS *vendor:123:*

# Посмотреть ключи API
KEYS *api_cache:*

# Получить TTL ключа
TTL catalog:categories_tree

# Получить значение
GET catalog:brands_all
```

## ⚠️ Важные замечания

1. **Автоматическая очистка работает:**
   - При сохранении/удалении Category
   - При сохранении/удалении Product
   - Рекомендуется добавить очистку при создании Order

2. **При изменении настроек** (`business_settings`) нужно очищать кеш:
   ```bash
   php artisan cache:clear-app --type=all
   ```

3. **При деплое** рекомендуется:
   ```bash
   php artisan cache:clear-app --type=all --warmup
   ```

## 🎯 Дальнейшие улучшения

**Можно добавить кеширование:**
1. Результатов поиска (с коротким TTL)
2. Списков товаров с фильтрами
3. Настроек из `business_settings` (через обсервер)
4. Flash deals и акций
5. Отзывов на товары

**Рекомендации:**
- Мониторить использование памяти Redis
- Настроить eviction policy в Redis (например, `allkeys-lru`)
- Добавить логирование cache hit/miss для анализа
- Рассмотреть использование Redis Cluster для масштабирования

## 📚 Дополнительные ресурсы

- [Laravel Cache Documentation](https://laravel.com/docs/cache)
- [Redis Documentation](https://redis.io/documentation)
- [Redis Best Practices](https://redis.io/topics/optimization)
