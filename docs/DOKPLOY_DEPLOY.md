# Инструкция по деплою ManySales в Dokploy с Redis

## 📋 Предварительные требования

В Dokploy должны быть созданы и запущены:
- ✅ PostgreSQL база данных (`postgreszakaz-rqk84o`)
- ✅ Redis сервер (`redis-8ugjdw`)

## 🚀 Пошаговая инструкция

### Шаг 1: Подготовка репозитория

Убедитесь, что в вашем Git репозитории есть:
- `Dockerfile` - уже содержит установку PHP Redis расширения
- `docker-compose.yml` - обновлен с настройками Redis
- `.env.dokploy` - файл с переменными окружения

### Шаг 2: Создание приложения в Dokploy

1. Откройте Dokploy Dashboard
2. Перейдите в раздел **Projects**
3. Нажмите **Create Application**
4. Выберите **Git Repository**
5. Введите URL вашего репозитория
6. Выберите ветку (например, `main`)

### Шаг 3: Настройка переменных окружения

В разделе **Environment Variables** приложения добавьте переменные из файла `.env.dokploy`:

**Критически важные для Redis:**
```env
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_DRIVER=redis
REDIS_HOST=redis-8ugjdw
REDIS_PASSWORD=DiMa198704@
REDIS_PORT=6379
REDIS_CLIENT=phpredis
```

**База данных:**
```env
DB_CONNECTION=pgsql
DB_HOST=postgreszakaz-rqk84o
DB_PORT=5432
DB_DATABASE=manysales
DB_USERNAME=user
DB_PASSWORD=tovar-zakaz-app2025@
```

**Основные:**
```env
APP_NAME=ManySales
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:9u2FLzN8Evlg47CKbL9M5ALst2CsIl7fymttM/7yxhw=
APP_URL=https://your-domain.com
```

> **Примечание:** Скопируйте ВСЕ переменные из `.env.dokploy` в Dokploy

### Шаг 4: Настройка домена и SSL

1. В разделе **Domains** добавьте ваш домен
2. Активируйте **SSL Certificate** (Let's Encrypt)
3. Сохраните изменения

### Шаг 5: Первый деплой с миграциями

**Только для первого деплоя:**

1. В переменных окружения установите:
   ```env
   MIGRATE_ON_START=true
   ```

2. Нажмите **Deploy**

3. Дождитесь завершения сборки и деплоя

4. **ВАЖНО:** После успешного деплоя верните:
   ```env
   MIGRATE_ON_START=false
   ```

   Это предотвратит запуск миграций при каждом деплое.

### Шаг 6: Проверка работы Redis

После деплоя проверьте, что Redis работает:

1. **Откройте ваш сайт** - вы должны быть автоматически разлогинены (это нормально!)

2. **Проверьте логи приложения** в Dokploy:
   ```bash
   # Должно быть
   Application Ready
   ```

3. **Проверьте API с заголовком** (через браузер DevTools или curl):
   ```bash
   curl -I https://your-domain.com/api/v1/categories

   # Должны увидеть заголовки:
   X-Cache: MISS  # первый запрос
   X-Cache: HIT   # повторные запросы
   ```

### Шаг 7: Прогрев кеша (опционально)

Подключитесь к контейнеру через Dokploy Terminal:

```bash
# Войти в контейнер
cd /var/www/html

# Прогреть кеш
php artisan cache:clear-app --type=catalog --warmup

# Проверить конфигурацию
php artisan config:cache
php artisan route:cache
```

## 🔧 Управление кешем после деплоя

### Очистка кеша через Dokploy Terminal:

```bash
# Очистить весь кеш
php artisan cache:clear-app --type=all

# Очистить только каталог
php artisan cache:clear-app --type=catalog

# Очистить API кеш
php artisan cache:clear-app --type=api

# Очистить кеш продавца
php artisan cache:clear-app --type=vendor --vendor=123

# Очистить с прогревом
php artisan cache:clear-app --type=all --warmup
```

### Автоматическая очистка кеша

Кеш автоматически очищается при:
- Создании/изменении категории
- Создании/изменении товара
- Создании заказа (для статистики продавца)

## 📊 Проверка производительности

### До Redis:
```
Загрузка главной: ~300-500ms
Открытие категории: ~200-400ms
API запрос: ~200-400ms
```

### После Redis:
```
Загрузка главной: ~100-200ms (↓2-3x)
Открытие категории: ~50-100ms (↓3-4x)
API запрос: ~50-100ms (↓3-4x)
```

## 🐛 Решение проблем

### Проблема: Приложение не подключается к Redis

**Решение:**
1. Проверьте, что Redis сервер запущен в Dokploy
2. Убедитесь, что `REDIS_HOST` соответствует Internal Host из Dokploy
3. Проверьте пароль в `REDIS_PASSWORD`

### Проблема: Ошибка "No connection could be made"

**Решение:**
1. Откройте настройки Redis в Dokploy
2. Скопируйте **Internal Host** (например, `redis-8ugjdw`)
3. Обновите `REDIS_HOST` в переменных окружения приложения
4. Пересоберите приложение

### Проблема: PHP расширение Redis не установлено

**Решение:**
Dockerfile уже содержит установку Redis:
```dockerfile
# Установка Redis расширения
RUN pecl install redis && docker-php-ext-enable redis
```

Если возникла ошибка:
1. Проверьте логи сборки в Dokploy
2. Убедитесь, что используется правильный Dockerfile
3. Пересоберите приложение: **Redeploy**

### Проблема: Сессии не сохраняются

**Решение:**
1. Проверьте `SESSION_DRIVER=redis` в переменных
2. Очистите кеш: `php artisan cache:clear`
3. Перезапустите приложение в Dokploy

## 📝 Проверочный список перед деплоем

- [ ] Redis сервер создан и запущен в Dokploy
- [ ] PostgreSQL база данных создана и доступна
- [ ] Все переменные окружения из `.env.dokploy` добавлены
- [ ] `REDIS_HOST` соответствует Internal Host вашего Redis
- [ ] `REDIS_PASSWORD` правильный
- [ ] Домен настроен и SSL активирован
- [ ] `MIGRATE_ON_START=true` для первого деплоя
- [ ] После деплоя: `MIGRATE_ON_START=false`

## 🔄 Процесс обновления (последующие деплои)

1. Внесите изменения в код
2. Закоммитьте и запушьте в Git
3. В Dokploy нажмите **Redeploy**
4. Дождитесь завершения
5. (Опционально) Очистите кеш если нужно:
   ```bash
   php artisan cache:clear-app --type=all
   ```

## 📚 Полезные команды

```bash
# Проверить подключение к Redis
redis-cli -h redis-8ugjdw -a DiMa198704@ ping
# Ответ: PONG

# Посмотреть ключи в Redis
redis-cli -h redis-8ugjdw -a DiMa198704@ KEYS *

# Посмотреть информацию о Redis
redis-cli -h redis-8ugjdw -a DiMa198704@ INFO

# Проверить расширение PHP Redis
php -m | grep redis
# Ответ: redis

# Проверить версию
php -r "echo phpversion('redis');"
```

## 🎯 Ожидаемый результат

После успешного деплоя:
- ✅ Приложение доступно по вашему домену
- ✅ SSL сертификат активен (зеленый замок)
- ✅ Пользователи могут входить и работать
- ✅ Сессии сохраняются в Redis
- ✅ Кеш каталога работает
- ✅ API возвращает заголовки `X-Cache`
- ✅ Производительность увеличена в 2-4 раза

## 💡 Дополнительная оптимизация

После деплоя рекомендуется:
1. Настроить мониторинг использования Redis
2. Добавить cron для периодической очистки старого кеша
3. Настроить queue worker для фоновых задач
4. Включить opcache для дополнительного ускорения PHP

Подробнее в `REDIS_OPTIMIZATION.md`
