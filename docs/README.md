# 📁 Документация и конфигурационные файлы

Эта папка содержит документацию и шаблоны конфигурации для деплоя приложения ManySales.

## 📂 Структура папки

```
docs/
├── .env.dokploy           ← ⚠️ ЛОКАЛЬНЫЙ файл (не в Git) - конфиг для Dokploy
├── .env.production        ← ⚠️ ЛОКАЛЬНЫЙ файл (не в Git) - production конфиг
├── .env.dokploy.example   ← ✅ Шаблон для Dokploy (БЕЗ секретов)
├── .env.example           ← ✅ Общий шаблон .env файла
├── SECURITY.md            ← 🔒 Инструкции по безопасности
└── README.md              ← 📖 Этот файл
```

## ⚠️ ВАЖНО: Безопасность

### Файлы которые НЕ попадают в Git:

- `.env.dokploy` - содержит **РЕАЛЬНЫЕ** пароли и ключи для Dokploy
- `.env.production` - содержит **РЕАЛЬНЫЕ** production секреты
- Любые файлы `.env.*` кроме `.env.*.example`

Эти файлы защищены в `.gitignore` и остаются только **ЛОКАЛЬНО**.

### Файлы которые можно коммитить:

- `.env.dokploy.example` - шаблон БЕЗ реальных секретов
- `.env.example` - общий шаблон
- `SECURITY.md` - инструкции по безопасности
- `README.md` - документация

## 🚀 Использование шаблонов

### Для деплоя через Dokploy:

1. Откройте `docs/.env.dokploy.example`
2. Скопируйте переменные в Dokploy → Applications → Environment
3. Замените все `YOUR_*` значения на реальные секреты
4. Сохраните в Dokploy (НЕ в файл!)

### Для локальной разработки:

1. Скопируйте `.env.example` в корень проекта как `.env`
2. Замените значения на локальные настройки
3. Сгенерируйте ключ: `php artisan key:generate`

### Для production через .env файл:

1. Скопируйте `docs/.env.dokploy` → `docs/.env.production`
2. Отредактируйте значения для вашего сервера
3. **НЕ КОММИТЬТЕ** этот файл в Git!

## 🔐 Генерация секретов

### APP_KEY (Laravel):
```bash
php artisan key:generate --show
```

### Сильный пароль (20+ символов):
```bash
# Linux/Mac
openssl rand -base64 32

# Windows PowerShell
-join ((48..57)+(65..90)+(97..122)|Get-Random -Count 24|%{[char]$_})
```

### Base64 кодирование (для ADMIN_IDENTIFIER):
```bash
# Linux/Mac
echo -n "your-email@example.com" | base64

# Windows PowerShell
[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("your-email@example.com"))
```

## 📚 Дополнительная документация

- [SECURITY.md](./SECURITY.md) - Инструкции по безопасности и восстановлению после утечки
- [Dokploy Documentation](https://docs.dokploy.com/)
- [Laravel Deployment](https://laravel.com/docs/deployment)

## ❓ Вопросы и поддержка

Если у вас возникли вопросы:
1. Прочитайте [SECURITY.md](./SECURITY.md)
2. Проверьте [Laravel документацию](https://laravel.com/docs)
3. Обратитесь к DevOps команде

---

**Последнее обновление:** 2025-12-12
