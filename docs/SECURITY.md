# 🔒 Инструкция по безопасности ManySales

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Смена паролей после утечки

Если конфиденциальные данные попали в Git репозиторий, **НЕМЕДЛЕННО** выполните все шаги ниже.

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС

### 1. ⚠️ Смена пароля PostgreSQL базы данных

```bash
# Подключитесь к PostgreSQL как администратор
psql -U postgres -h postgreszakaz-rqk84o

# Смените пароль для пользователя
ALTER USER user WITH PASSWORD 'НОВЫЙ_СИЛЬНЫЙ_ПАРОЛЬ_МИНИМУМ_20_СИМВОЛОВ';

# Отключитесь
\q
```

**Требования к паролю:**
- Минимум 20 символов
- Буквы (A-Z, a-z), цифры (0-9), спецсимволы (!@#$%^&*)
- Уникальный (не используется больше нигде)

**Пример генерации сильного пароля:**
```bash
# Linux/Mac
openssl rand -base64 32

# Windows PowerShell
[System.Convert]::ToBase64String([System.Security.Cryptography.RNGCryptoServiceProvider]::GetBytes(32))
```

### 2. ⚠️ Смена пароля Redis

```bash
# Подключитесь к Redis
redis-cli -h redis-8ugjdw

# Установите новый пароль
CONFIG SET requirepass "НОВЫЙ_СИЛЬНЫЙ_ПАРОЛЬ"

# Сохраните конфигурацию
CONFIG REWRITE

# Отключитесь
QUIT
```

### 3. ⚠️ Генерация нового APP_KEY

```bash
# Перейдите в директорию проекта
cd /path/to/manysales

# Сгенерируйте новый ключ шифрования
php artisan key:generate --show

# Скопируйте новый ключ и добавьте в .env или Dokploy Environment
```

**ВАЖНО:** После смены APP_KEY:
- Все зашифрованные данные станут недоступны
- Все сессии пользователей будут сброшены
- Cookies будут инвалидированы

### 4. ⚠️ Обновление переменных окружения в Dokploy

1. Зайдите в Dokploy → Applications → ManySales → Environment
2. Обновите следующие переменные:
   - `DB_PASSWORD` → новый пароль PostgreSQL
   - `REDIS_PASSWORD` → новый пароль Redis
   - `APP_KEY` → новый ключ из шага 3
3. Сохраните изменения
4. Перезапустите приложение

### 5. ⚠️ Обновление локальных .env файлов

Обновите следующие файлы **ЛОКАЛЬНО** (они не попадут в Git):
- `docs/.env.dokploy` - для деплоя через Dokploy
- `docs/.env.production` - резервная копия production конфига
- `.env` (корень проекта) - для локальной разработки

**НЕ КОММИТЬТЕ ЭТИ ФАЙЛЫ В GIT!**

---

## 🛡️ Проверка безопасности

### Проверьте логи на подозрительную активность

**PostgreSQL:**
```bash
# Проверьте последние подключения
psql -U postgres -c "SELECT * FROM pg_stat_activity WHERE datname='manysales';"

# Проверьте неудачные попытки входа (если включено логирование)
tail -n 100 /var/log/postgresql/postgresql-*.log | grep "FATAL"
```

**Redis:**
```bash
# Проверьте количество подключений
redis-cli -h redis-8ugjdw INFO clients

# Проверьте команды
redis-cli -h redis-8ugjdw MONITOR
```

**Laravel:**
```bash
# Проверьте логи приложения
tail -n 200 storage/logs/laravel.log | grep -i "error\|warning\|unauthorized"
```

### Проверьте изменения в базе данных

```sql
-- Проверьте последние изменения в таблице users
SELECT id, name, email, created_at, updated_at
FROM users
ORDER BY updated_at DESC
LIMIT 20;

-- Проверьте последние заказы
SELECT id, customer_id, order_amount, created_at
FROM orders
ORDER BY created_at DESC
LIMIT 20;

-- Проверьте настройки системы
SELECT * FROM business_settings
WHERE type IN ('company_email', 'company_phone', 'company_web_logo');
```

---

## 🔐 Очистка истории Git (ОПЦИОНАЛЬНО, НО РЕКОМЕНДУЕТСЯ)

**⚠️ ВНИМАНИЕ:** Это перезапишет историю репозитория!

### Вариант 1: Использование BFG Repo-Cleaner (рекомендуется)

```bash
# Установите BFG (https://rtyley.github.io/bfg-repo-cleaner/)
# Java должна быть установлена

# Создайте резервную копию
cp -r manysales manysales-backup

# Удалите файлы из истории
java -jar bfg.jar --delete-files '.env.dokploy' manysales
java -jar bfg.jar --delete-files '.env.production' manysales

# Очистите и упакуйте репозиторий
cd manysales
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# Force push в GitHub (ВНИМАНИЕ: перезапишет историю!)
git push origin --force --all
git push origin --force --tags
```

### Вариант 2: Использование git filter-repo

```bash
# Установите git-filter-repo
pip install git-filter-repo

# Создайте резервную копию
cp -r manysales manysales-backup

cd manysales

# Удалите файлы из истории
git filter-repo --path docs/.env.dokploy --invert-paths
git filter-repo --path docs/.env.production --invert-paths
git filter-repo --path .env.dokploy --invert-paths
git filter-repo --path .env.production --invert-paths

# Force push
git push origin --force --all
```

**После force push:**
- Все участники команды должны заново клонировать репозиторий
- Старые клоны будут несовместимы с новой историей

---

## 📝 Правила безопасности для разработчиков

### ✅ ВСЕГДА:

1. **Используйте переменные окружения** для всех секретов
2. **Проверяйте .gitignore** перед коммитом
3. **Используйте `git status`** перед `git add .`
4. **Генерируйте уникальные пароли** для каждого сервиса
5. **Храните бэкапы паролей** в менеджере паролей (1Password, Bitwarden, LastPass)

### ❌ НИКОГДА:

1. **НЕ коммитьте файлы `.env*`** (кроме `.env.example`)
2. **НЕ коммитьте пароли, ключи, токены** в код
3. **НЕ используйте `git add .`** без проверки
4. **НЕ используйте слабые пароли** типа "password123"
5. **НЕ используйте одинаковые пароли** для разных сервисов

### 🔍 Проверка перед коммитом:

```bash
# Проверьте что будет закоммичено
git status
git diff --cached

# Проверьте на наличие секретов
git diff --cached | grep -i "password\|secret\|key\|token"

# Если найдены секреты - НЕ КОММИТЬТЕ!
git reset HEAD <файл>
```

---

## 🚨 Что делать если снова произошла утечка

1. **НЕ ПАНИКУЙТЕ**
2. **НЕМЕДЛЕННО** смените все скомпрометированные пароли
3. **УДАЛИТЕ** конфиденциальные данные из репозитория
4. **ОЧИСТИТЕ** историю Git (см. выше)
5. **УВЕДОМИТЕ** команду о проблеме
6. **ПРОВЕРЬТЕ** логи на подозрительную активность

---

## 📚 Дополнительные ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [GitHub Secret Scanning](https://docs.github.com/en/code-security/secret-scanning)
- [BFG Repo-Cleaner](https://rtyley.github.io/bfg-repo-cleaner/)
- [git-filter-repo](https://github.com/newren/git-filter-repo)

---

## ✅ Чек-лист восстановления безопасности

- [ ] Сменен пароль PostgreSQL
- [ ] Сменен пароль Redis
- [ ] Сгенерирован новый APP_KEY
- [ ] Обновлены переменные в Dokploy
- [ ] Обновлены локальные .env файлы
- [ ] Проверены логи на подозрительную активность
- [ ] Проверена база данных на несанкционированные изменения
- [ ] (Опционально) Очищена история Git
- [ ] (Опционально) Выполнен force push
- [ ] Команда уведомлена о проблеме

---

**Дата создания:** 2025-12-12
**Версия:** 1.0
**Автор:** DevOps Team
